module github.com/caracolazuldev/markdown-sync

go 1.21

// module requirements are managed by the Go tool; specific pinned pseudo-versions
// were removed because they referenced an unknown revision. Use `go get` or
// `go mod tidy` to add appropriate versions.

require golang.org/x/oauth2 v0.26.0

require cloud.google.com/go/compute/metadata v0.3.0 // indirect
